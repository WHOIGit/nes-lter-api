# How to install client package

## Create and activate virtual environment

Using venv:

```
python -m venv .venv
source .venv/bin/activate
```

## Install package in activated environment

Install `poetry`

```
pip install poetry
```

Use `poetry` to install package

```
poetry install
```
